// Arrow function 

// write a arrow fun to calculate add of 2 num..
const add = (a, b) => {
    console.log("sum of "+a+" and " +b+ " is " + (a+b))
}
add(5, 2);

// ..arrow fun.to check if a string contains special char or not
const special = () => {
   
}